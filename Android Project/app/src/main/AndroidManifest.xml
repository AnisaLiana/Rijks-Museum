<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rijksmuseum">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".com.rijksmuseum.login.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

         <!-- <activity android:name=".RegisterActivity"></activity> -->
        <!-- <activity android:name=".Page1Activity" /> &lt;!&ndash; <activity android:name=".activity_home"></activity> &ndash;&gt; -->
        <activity android:name=".MainActivity" />
        <activity android:name=".activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Page1Activity"
            android:label="@string/title_activity_page1"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--<activity android:name=".ProfileMenu"></activity>-->
        <!--<activity android:name=".HomeMenu" />-->
        <!--<activity android:name=".Root" />-->
        <activity android:name=".RegisterActivity" />


    </application>

</manifest>